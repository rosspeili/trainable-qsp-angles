%% ============================================================
%%  V. Peilivanidis — ARPA Hellenic Logical Systems
%% ============================================================
\pdfoutput=1
\documentclass[11pt,a4paper]{article}

%% --- Font stack  ---
\usepackage[T1]{fontenc}
\usepackage[utf8]{inputenc}
\usepackage{amsmath, amssymb, amsthm}
\usepackage{newpxtext, newpxmath}
\usepackage[scaled=0.88]{inconsolata}
\usepackage{microtype}

%% --- Page geometry ---
\usepackage[a4paper, left=3.0cm, right=3.0cm, top=3.2cm, bottom=3.2cm]{geometry}

%% --- Color palette ---
\usepackage{xcolor}
\definecolor{accent}{HTML}{1F4E8C}
\definecolor{coderule}{HTML}{C8D8EE}
\definecolor{codebg}{HTML}{F4F7FC}
\definecolor{boxbg}{HTML}{EDF2FA}
\definecolor{boxrule}{HTML}{1F4E8C}
\definecolor{graytext}{HTML}{555555}
\definecolor{lightrule}{HTML}{CCCCCC}

%% --- Hyperlinks ---
\usepackage[colorlinks=true, citecolor=accent, linkcolor=accent,
            urlcolor=accent, breaklinks=true]{hyperref}
\usepackage{url}

%% --- Math ---
\usepackage{bm}

%% --- Graphics ---
\usepackage{graphicx}
\usepackage{booktabs}
\usepackage{tabularx}
\usepackage{float}

%% --- TikZ + PGFPlots ---
\usepackage{tikz}
\usetikzlibrary{arrows.meta, shapes.geometric, shapes.misc,
                positioning, calc, fit, backgrounds}
\usepackage{pgfplots}
\pgfplotsset{compat=1.18}
\usepgfplotslibrary{groupplots,statistics}

%% --- Captions ---
\usepackage[
  font={small, color=graytext},
  labelfont={bf, color=black},
  labelsep=period,
  justification=justified,
  singlelinecheck=false
]{caption}

%% --- Section titles ---
\usepackage{titlesec}
\titleformat{\section}
  {\normalfont\large\bfseries}{\thesection}{0.7em}{}
  [\vspace{2pt}{\color{accent}\hrule height 0.5pt}\vspace{2pt}]
\titleformat{\subsection}
  {\normalfont\normalsize\bfseries}{\thesubsection}{0.7em}{}
\titleformat{\subsubsection}
  {\normalfont\normalsize\itshape}{\thesubsubsection}{0.7em}{}
\titlespacing{\section}{0pt}{14pt plus 4pt minus 2pt}{6pt plus 2pt}
\titlespacing{\subsection}{0pt}{10pt plus 3pt minus 1pt}{4pt plus 1pt}

%% --- Header / footer ---
\usepackage{fancyhdr}
\pagestyle{fancy}
\fancyhf{}
\fancyhead[L]{\small\color{graytext}V. Peilivanidis}
\fancyhead[R]{\small\color{graytext}Learning QSP Phase Angles --- Benchmark \& Implementation Note}
\fancyfoot[C]{\small\thepage}
\renewcommand{\headrulewidth}{0.3pt}
\renewcommand{\footrulewidth}{0pt}

\fancypagestyle{firstpage}{
  \fancyhf{}
  \fancyfoot[C]{\small\thepage}
  \renewcommand{\headrulewidth}{0pt}
  \renewcommand{\footrulewidth}{0pt}
}

%% --- Contribution box ---
\usepackage{tcolorbox}
\tcbuselibrary{skins, breakable}
\tcbset{
  contribbox/.style={
    enhanced, breakable,
    colback=boxbg, colframe=accent,
    leftrule=3pt, toprule=0.4pt, bottomrule=0.4pt, rightrule=0.4pt,
    arc=2pt, outer arc=2pt,
    left=8pt, right=8pt, top=6pt, bottom=6pt,
    fonttitle=\small\bfseries\color{accent},
    title={Summary of Contributions},
    attach boxed title to top left={yshift=-2mm, xshift=6pt},
    boxed title style={colback=white, colframe=white}
  }
}

%% --- Code listings ---
\usepackage{listings}
\lstdefinestyle{qspcode}{
  language=Python,
  basicstyle=\small\ttfamily,
  keywordstyle=\color{accent}\bfseries,
  stringstyle=\color{graytext},
  commentstyle=\color{graytext}\itshape,
  backgroundcolor=\color{codebg},
  frame=single,
  rulecolor=\color{coderule},
  framexleftmargin=4pt,
  xleftmargin=4pt,
  xrightmargin=2pt,
  numbers=left,
  numberstyle=\tiny\color{graytext},
  numbersep=8pt,
  breaklines=true,
  showstringspaces=false,
  tabsize=4,
  aboveskip=8pt, belowskip=6pt
}
\lstset{style=qspcode}

%% --- Bibliography ---
\usepackage[style=numeric-comp, backend=biber,
            sortcites=true, giveninits=true,
            maxcitenames=2, maxbibnames=99,
            doi=true, url=false, isbn=false]{biblatex}
\addbibresource{references.bib}

%% --- Line spacing ---
\setlength{\parskip}{4pt}
\setlength{\parindent}{1em}

%% ============================================================
%%  METADATA
%% ============================================================
\title{%
  \vspace{-0.5cm}%
  {\LARGE\bfseries Learning Quantum Signal Processing Phase Angles%
  \\ via Gradient Descent}\\[0.55em]
  {\normalsize\normalfont\color{accent}
   A Reproducible Benchmark and JAX Implementation Note}
}

\author{%
  \textbf{Vladimiros Peilivanidis}\\[0.25em]
  {\small ARPA Hellenic Logical Systems, Thessaloniki, Greece}\\
  {\small \href{mailto:r1@arpacorp.net}{\texttt{r1@arpacorp.net}}}
  \quad{\small ORCID: \href{https://orcid.org/0009-0003-0121-465X}{0009-0003-0121-465X}}
}

\date{\small June 2026}

\input{manuscript_numbers.tex}

\begin{document}

\maketitle
\thispagestyle{firstpage}

%% arpa_logo.png: local paper asset only (gitignored; not on GitHub)
\begin{tikzpicture}[remember picture, overlay]
  \node[anchor=north west, xshift=1.2cm, yshift=-1.2cm] at (current page.north west) {%
    \includegraphics[height=1.15cm]{arpa_logo.png}};
\end{tikzpicture}

\begin{center}
\begin{tcolorbox}[
  enhanced, arc=3pt, boxrule=0.4pt,
  colback=boxbg, colframe=coderule,
  left=8pt, right=8pt, top=4pt, bottom=4pt,
  width=0.92\textwidth, halign=center
]
{\small\color{graytext}%
 DOI\ \href{https://doi.org/10.5281/zenodo.20645403}{\texttt{10.5281/zenodo.20645403}}
 \quad|\quad
 Code\ \href{https://github.com/rosspeili/trainable-qsp-angles}{\texttt{github.com/rosspeili/trainable-qsp-angles}}
 \quad|\quad
 ORCID\ \href{https://orcid.org/0009-0003-0121-465X}{\texttt{0009-0003-0121-465X}}}
\end{tcolorbox}
\end{center}

%% Abstract environment
\begin{center}
\begin{minipage}{0.92\textwidth}
\small
\textbf{Abstract.}\quad
This report documents a \emph{reproducible benchmark} and reference implementation for learning Quantum Signal Processing (QSP) phase angles by gradient descent on a flat, JAX-traceable circuit (PennyLane is reference-only). We do not propose a new QSP algorithm; we measure when gradient training is preferable to mapped analytic solvers on a shared flat-circuit metric. On a standard degree-5 Chebyshev approximation of $\sin(x)$, Adam reaches train MSE $9.6\times10^{-5}$ for seed~0---better than mapped analytic baselines ($\approx4.7\times10^{-3}$) on the same circuit---and 30/30 random seeds converge below $10^{-3}$. We release degree-scaling tables, a phase-convention mapping protocol between solvers and the training circuit, audit logs, and an implementation note on a QSVT-template pitfall that breaks JAX autodiff. All scripts and hardcoded figure values are under Apache~2.0.

\vspace{6pt}
\noindent\textbf{Keywords:}\quad
quantum signal processing (QSP) $\cdot$
reproducible benchmark $\cdot$
quantum singular value transformation (QSVT) $\cdot$
phase angle optimization $\cdot$
differentiable quantum circuits $\cdot$
gradient descent $\cdot$
Chebyshev polynomial approximation $\cdot$
automatic differentiation $\cdot$
JAX $\cdot$
differentiable quantum programming
\end{minipage}
\end{center}

\vspace{0.5em}
\noindent\rule{\textwidth}{0.3pt}
\vspace{0.3em}

%% ============================================================
\section{Introduction}
%% ============================================================

The history of quantum algorithms is, in large part, a history of discovering that quantum mechanics permits a surprisingly rich set of mathematical operations to be performed efficiently on physical hardware. Among the most elegant of such discoveries is \emph{Quantum Signal Processing} (QSP) --- a framework establishing a precise and complete characterization of the polynomial transformations achievable by a single-qubit quantum circuit interleaved with repeated queries to a signal oracle~\cite{low2017optimal}.

The core insight of QSP is deceptively simple. Given a scalar signal $x \in [-1, 1]$ encoded in the matrix elements of a unitary oracle $W(x)$, a circuit that alternates between calls to $W(x)$ and single-qubit $Z$-rotations (the \emph{phase angles}) encodes a polynomial in $x$ in the expectation value of the final state. The converse is equally powerful: for any real polynomial satisfying mild bounded-norm and parity conditions, there exist phase angles that realize it exactly. This completeness result~\cite{low2017optimal, gilyen2019quantum} elevates QSP to the status of a \emph{universal language} for quantum algorithms.

The implications are substantial. Algorithms for Hamiltonian simulation, quantum linear systems, amplitude amplification, quantum phase estimation, and ground-state preparation can all be expressed as instances of QSP or its multi-qubit generalization, Quantum Singular Value Transformation (QSVT)~\cite{martyn2021grand, gilyen2019quantum}. This ``grand unification'' perspective reveals that many apparently unrelated quantum algorithms are in fact the same circuit architecture with different phase angle sequences --- turning quantum algorithm design into a problem of polynomial approximation.

\subsection{The Phase Angle Problem}

The practical challenge is computing those phase angles. Given a target polynomial $P(x)$, classical algorithms exist to recover the exact phase angles $(\phi_0, \phi_1, \ldots, \phi_d)$. However, these analytic solvers are numerically fragile at high polynomial degree~\cite{haah2019product, chao2020finding}, and they require the target polynomial to be known in closed form --- an assumption that breaks down in variational settings where the target is implicitly defined by a downstream loss function.

\subsection{This Work}

\textbf{Scope.} This is a \emph{benchmark and implementation note}, not a new QSP theory or solver. Gradient descent on phase angles is already known in variational settings; our contribution is to \emph{measure}, \emph{document}, and \emph{release} a reproducible protocol for when that route beats mapped analytic baselines on a flat, JAX-traceable circuit.

We explore learning phase angles from random initialization using gradient descent. The key observation is that a QSP circuit, when implemented correctly as a differentiable quantum program, admits automatic differentiation through all its parameters---making it possible to minimize any differentiable loss over the phase angles with standard optimizers.

We demonstrate this via a publicly available reference implementation~\cite{peili_trainable_qsp_2026} using PennyLane~\cite{pennylane2022} with JAX~\cite{jax2018github}; the underlying flat-circuit recipe transfers to other SDKs such as Qiskit~\cite{qiskit2024}, Cirq~\cite{cirq2024}, or TensorFlow Quantum~\cite{broughton2020tfq} when parameters remain live in the autodiff graph. We identify and document a non-obvious incompatibility between template-based QSVT constructors and JAX tracing in our PennyLane code path, and provide the correct flat-circuit pattern.

\begin{tcolorbox}[contribbox]
\begin{itemize}\setlength\itemsep{3pt}
  \item[\textbf{C1.}] \textbf{Implementation note:} high-level QSVT template APIs can capture operator values at circuit-build time, breaking JAX differentiability through phase angles (observed in PennyLane's \texttt{qml.QSVT}); we provide a flat-circuit workaround that preserves end-to-end gradient flow.
  \item[\textbf{C2.}] \textbf{Benchmark artifacts:} a reproducible pipeline (30-seed statistics, degree scaling, hyperparameter ablation) for a degree-5 Chebyshev $\sin$ target, reporting convergence from MSE $\approx1.36$ to $9.6\times10^{-5}$ in 500 Adam steps (reference seed).
  \item[\textbf{C3.}] \textbf{Convention protocol:} fair comparison of gradient training to analytic baselines via mapped flat-circuit evaluation ($\phi_{\mathrm{flat}}=\pi/2-\phi_{\mathrm{chao}}$), with an updated decision guide grounded in measured trade-offs.
  \item[\textbf{C4.}] \textbf{Open release:} code, experiment logs, audit trail, hardcoded figure values, and an interactive notebook under Apache~2.0 at \href{https://github.com/rosspeili/trainable-qsp-angles}{\texttt{github.com/rosspeili/trainable-qsp-angles}}.
\end{itemize}
\end{tcolorbox}

%% ============================================================
\section{Background: Quantum Signal Processing}
%% ============================================================

\subsection{Signal Oracle}

The standard QSP signal oracle $W(x)$ encodes a scalar $x \in (-1,1)$ in its matrix elements:
\begin{equation}
  W(x) \;=\; H\,R_Z(-2\arccos x)\,H
  \;=\;
  \begin{pmatrix} x & i\sqrt{1-x^2} \\ i\sqrt{1-x^2} & x \end{pmatrix},
\end{equation}
where $H$ is the Hadamard gate and $R_Z(\theta)=e^{-i\theta Z/2}$. The signal $x$ appears in the top-left matrix element, accessible as a quantum amplitude. Geometrically, $W(x)$ rotates the Bloch sphere state by angle $\theta = 2\arccos(x)$ around the $x$-axis, mapping $|0\rangle$ to $x|0\rangle + i\sqrt{1-x^2}|1\rangle$ (Figure~\ref{fig:bloch}).

\begin{figure}[ht]
\centering
\begin{tikzpicture}[scale=1.75, >=Stealth, line cap=round]

  \fill[accent!5] (0,0) circle (1);
  \draw[gray!50, thin] (0,0) circle (1);

  \draw[dashed, gray!35, thin] (1,0) arc (0:180:1 and 0.30);
  \draw[gray!45, thin] (-1,0) arc (180:360:1 and 0.30);

  \draw[dashed, gray!30, thin] (0,1) arc (90:270:0.30 and 1);
  \draw[gray!40, thin] (0,-1) arc (270:450:0.30 and 1);

  \draw[->, thin, gray!70] (0,0,0) -- (1.28,0) node[right, font=\scriptsize, graytext] {$|{+}\rangle$};
  \draw[->, thin, gray!70] (0,0,0) -- (-1.28,0) node[left, font=\scriptsize, graytext] {$|{-}\rangle$};
  \draw[->, thin, gray!55] (0,0) -- (-0.42,-0.20) node[below left, font=\scriptsize, graytext] {$y$};
  \draw[->, thin, gray!60] (0,0) -- (0, 1.30) node[above, font=\small, graytext] {$z$};

  \fill[accent!50] (0,1) circle (1.8pt);
  \node[above right, font=\small\bfseries, accent] at (0,1) {$|0\rangle$};
  \fill[gray!70] (0,-1) circle (1.8pt);
  \node[below right, font=\small, graytext] at (0,-1) {$|1\rangle$};

  \draw[accent!40, dashed, thick] (0,1) arc (90:30:1);

  \draw[->, ultra thick, accent!35] (0,0) -- (0,1);

  \draw[->, ultra thick, accent] (0,0) -- ({sin(60)},{cos(60)});
  \fill[accent] ({sin(60)},{cos(60)}) circle (2.2pt);
  \node[right, font=\small\bfseries, accent] at ({sin(60)+0.04},{cos(60)})
    {$W(x)|0\rangle$};

  \draw[thin, graytext] (0,0.28) arc (90:30:0.28);
  \node[font=\scriptsize, graytext] at (0.13, 0.35) {$\theta$};

  \draw[thin, dashed, gray!35] ({sin(60)},{cos(60)}) -- ({sin(60)},0)
    node[below, font=\scriptsize, graytext] {$\sqrt{1-x^2}$};
  \draw[thin, dashed, gray!35] (0,{cos(60)}) -- ({sin(60)},{cos(60)});
  \node[left, font=\scriptsize, graytext] at (0,{cos(60)+0.02}) {$x$};

\end{tikzpicture}
\caption{Bloch sphere representation of the QSP signal oracle $W(x)$. The oracle rotates the initial state $|0\rangle$ (faint arrow) by angle $\theta = 2\arccos(x)$ around the $x$-axis, producing the state $W(x)|0\rangle = x|0\rangle + i\sqrt{1-x^2}|1\rangle$ (solid arrow). The signal $x$ appears as the $z$-projection (amplitude on $|0\rangle$), and $\sqrt{1-x^2}$ as the transverse component.}
\label{fig:bloch}
\end{figure}

\subsection{The QSP Sequence}


A degree-$d$ QSP circuit interleaves $d$ oracle calls with $d+1$ phase rotations $\boldsymbol{\phi}=(\phi_0,\ldots,\phi_d)\in\mathbb{R}^{d+1}$:
\begin{equation}
  U_{\mathrm{QSP}}(x;\boldsymbol{\phi})
  \;=\; R_Z(-2\phi_0)\prod_{k=1}^{d} W(x)\,R_Z(-2\phi_k).
\end{equation}
The expectation value $\langle X\rangle = \langle 0|U^\dagger X\,U|0\rangle$ is a degree-$d$ polynomial in $x$ determined entirely by $\boldsymbol{\phi}$.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[>=Stealth, x=1.05cm, y=1cm, line cap=round]

  \def\W{0.80}
  \def\H{0.44}

  \draw[line width=1.2pt] (-0.9,0) -- (12.1,0);

  \node[left, font=\normalsize\bfseries] at (-0.9,0) {$|0\rangle$};

  \foreach \cx/\lbl/\sub in {
    0.85/{$R_z$}/{$\!-2\phi_0$},
    4.95/{$R_z$}/{$\!-2\phi_1$},
    9.65/{$R_z$}/{$\!-2\phi_d$}}
  {
    \fill[black!8, draw=black!70, rounded corners=3pt, line width=0.9pt]
      (\cx-\W, -\H) rectangle (\cx+\W, \H);
    \node[font=\small\bfseries, text=black!80] at (\cx, 0.10) {\lbl};
    \node[font=\scriptsize, text=black!60] at (\cx,-0.20) {\sub};
  }

  \foreach \cx in {2.90, 7.40} {
    \fill[accent!14, draw=accent, rounded corners=3pt, line width=1.1pt]
      (\cx-0.70,-\H) rectangle (\cx+0.70,\H);
    \node[font=\small\bfseries, text=accent] at (\cx, 0.10) {$W(x)$};
    \node[font=\scriptsize, text=accent!80] at (\cx,-0.20) {oracle};
  }

  \node[font=\large, text=graytext] at (6.20, 0) {$\cdots$};

  \begin{scope}[shift={(11.35,0)}]
    \fill[accent!10, draw=accent!60, rounded corners=3pt, line width=0.9pt]
      (-0.62,-\H) rectangle (0.62,\H);
    \draw[accent!80, line width=0.8pt]
      (-0.50,-0.30) arc (180:0:0.50 and 0.25);
    \draw[->, accent, line width=0.9pt]
      (0,-0.30) -- (0.38, 0.22);
    \node[font=\scriptsize, text=accent!80] at (0, -0.36) {measure};
  \end{scope}

  \draw[<->, thick, graytext!70] (1.65,-0.85) -- (10.45,-0.85)
    node[midway, below, font=\small, text=graytext]
    {$P_{\boldsymbol{\phi}}(x)$ — degree-$d$ polynomial in $x$};
  \draw[dashed, graytext!50, thin] (1.65,-0.46) -- (1.65,-0.85);
  \draw[dashed, graytext!50, thin] (10.45,-0.46) -- (10.45,-0.85);

\end{tikzpicture}
\caption{QSP circuit for degree $d$. Gray boxes are trainable phase rotations $R_z(-2\phi_k)$; blue boxes are signal oracle calls $W(x)$. The final measurement yields the expectation value $\langle X\rangle = P_{\boldsymbol{\phi}}(x)$, a degree-$d$ polynomial in $x$ fully determined by the phase angle sequence $\boldsymbol{\phi}$.}
\label{fig:qsp_circuit}
\end{figure}


\subsection{Completeness Theorem}

The central result of QSP theory~\cite{low2017optimal, martyn2021grand} establishes a bijection between phase sequences and polynomials:

\begin{quote}\itshape
For any real polynomial $P$ of degree $d$ satisfying (i) $|P(x)|\le 1$ for all $x\in[-1,1]$, and (ii) $P$ has definite parity, there exist $(\phi_0,\ldots,\phi_d)\in\mathbb{R}^{d+1}$ such that $\langle X\rangle = P(x)$ for all $x\in(-1,1)$.
\end{quote}

The QSVT generalization~\cite{gilyen2019quantum} extends this to matrix-valued signals, enabling block-encoded polynomial transformations of arbitrary Hermitian operators. Table~\ref{tab:algorithms} lists representative quantum algorithms expressible as QSP instances.

\begin{table}[ht]
\centering
\small
\begin{tabularx}{\textwidth}{@{} l X l @{}}
  \toprule
  \textbf{Algorithm} & \textbf{Target polynomial} & \textbf{Reference} \\
  \midrule
  Hamiltonian simulation    & $e^{-iHt}$ (Jacobi-Anger expansion)   & \cite{low2019hamiltonian} \\
  Amplitude amplification   & Step-function approximation            & \cite{martyn2021grand} \\
  Quantum phase estimation  & Chebyshev spectral filter              & \cite{martyn2021grand} \\
  Ground-state preparation  & Low-pass spectral filter               & \cite{dong2021efficient} \\
  Quantum chemistry         & Matrix inversion ${\sim}1/x$           & \cite{babbush2018encoding} \\
  \bottomrule
\end{tabularx}
\caption{Quantum algorithms expressible as QSP instances. Each algorithm corresponds to a target polynomial; the phase angles provide the circuit that implements it.}
\label{tab:algorithms}
\end{table}

%% ============================================================
\section{Methods: Gradient-Based Phase Angle Learning}
%% ============================================================

\subsection{Optimization Objective}

Given a target polynomial $P_{\mathrm{target}}(x)$ satisfying QSP admissibility conditions, we minimize the mean squared error over a discrete signal grid $\{x_i\}_{i=1}^N \subset (-1,1)$:
\begin{equation}
  \boldsymbol{\phi}^* = \arg\min_{\boldsymbol{\phi}}\;\mathcal{L}(\boldsymbol{\phi})
  = \frac{1}{N}\sum_{i=1}^{N}\bigl(\langle X\rangle_{\boldsymbol{\phi}}(x_i) - P_{\mathrm{target}}(x_i)\bigr)^2.
  \label{eq:loss}
\end{equation}
Figure~\ref{fig:pipeline} illustrates the full training pipeline.

\begin{figure}[ht]
\centering
\begin{tikzpicture}[>=Stealth,
  box/.style={draw=accent, rounded corners=4pt, fill=white, thick,
              minimum width=2.6cm, minimum height=0.75cm,
              font=\small, align=center},
  lossbox/.style={draw=accent, rounded corners=4pt, fill=codebg, thick,
                  minimum width=2.4cm, minimum height=0.75cm,
                  font=\small\bfseries, align=center},
  tgtbox/.style={draw=graytext, dashed, rounded corners=4pt, fill=white,
                 minimum width=2.4cm, minimum height=0.75cm,
                 font=\small, align=center},
  fwd/.style={-Stealth, thick, accent},
  bwd/.style={-Stealth, thick, black!45, dashed},
  garr/.style={-Stealth, thin, graytext},
  lbl/.style={font=\scriptsize, text=graytext}
]

  \node[box]     (phases)  at (0,    0)   {Phase angles $\boldsymbol{\phi}$};
  \node[box]     (circuit) at (3.8,  0)   {QSP Circuit $U(\boldsymbol{\phi})$};
  \node[box]     (output)  at (7.6,  0)   {Output $\langle X\rangle_{\boldsymbol{\phi}}(x_i)$};

  \node[box]     (adam)    at (0,   -2.0) {Adam Update};
  \node[tgtbox]  (target)  at (3.8, -2.0) {Target $P_{\mathrm{target}}(x)$};
  \node[lossbox] (loss)    at (7.6, -2.0) {MSE Loss $\mathcal{L}$};

  \node[above=0.3cm of circuit, lbl] (sig) {signal grid $\{x_i\}$};

  \draw[fwd]  (phases)  -- (circuit);
  \draw[fwd]  (circuit) -- (output);
  \draw[garr] (sig)     -- (circuit);

  \draw[fwd]  (output)  -- (loss);
  \draw[fwd]  (target)  -- (loss);

  \coordinate (lb) at ([yshift=-0.45cm]loss.south);
  \coordinate (ab) at ([yshift=-0.45cm]adam.south);
  \draw[bwd] (loss.south) -- (lb) --
    node[below, lbl]{$\partial\mathcal{L}/\partial\boldsymbol{\phi}$}
    (ab) -- (adam.south);

  \draw[bwd] (adam.north) -- (phases.south);

\end{tikzpicture}
\caption{Gradient training pipeline. \textbf{Forward pass} (solid): phase angles $\boldsymbol{\phi}$ and signal grid $\{x_i\}$ enter the QSP circuit; the output is compared against the target polynomial in the MSE loss. \textbf{Backward pass} (dashed): gradients $\partial\mathcal{L}/\partial\boldsymbol{\phi}$ propagate via Adam to update $\boldsymbol{\phi}$.}
\label{fig:pipeline}
\end{figure}


\subsection{Target Polynomial}

The target is a degree-5 Chebyshev expansion of $\sin(x)$ using odd polynomials $T_k$ of the first kind:
\begin{equation}
  P_{\mathrm{target}}(x) = c_1 T_1(x) + c_3 T_3(x) + c_5 T_5(x),
\end{equation}
with $c_1=0.9775$, $c_3=-0.1564$, $c_5=0.0158$. This polynomial is odd, bounded in $[-1,1]$, and satisfies the QSP parity condition. Maximum deviation from $\sin(x)$ is $0.1738$ --- a degree-5 truncation artifact, not a QSP limitation.

\subsection{Circuit Implementation and the JAX Traceability Constraint}

High-level QSVT template constructors---including PennyLane's \texttt{qml.QSVT}---can build operator objects at circuit definition time, capturing concrete numerical values that exit JAX's abstract trace and produce zero gradients through the phase angles. The fix is a \emph{flat circuit} of primitive gates, keeping phase angles as live JAX arrays throughout. The same principle applies in other frameworks whenever templates materialize parameters before autodiff sees them.

\begin{lstlisting}[caption={Reference flat-circuit QSP implementation (PennyLane + JAX). The structure is portable; only the gate API differs in Qiskit, Cirq, or TFQ.}, label={lst:circuit}]
import pennylane as qp

@qp.qnode(dev, interface="jax", diff_method="backprop")
def qsp_qnode(phases, x):
    qp.RZ(-2.0 * phases[0], wires=0)
    for k in range(1, len(phases)):
        qp.Hadamard(wires=0)
        qp.RZ(-2.0 * jnp.arccos(x), wires=0)
        qp.Hadamard(wires=0)
        qp.RZ(-2.0 * phases[k], wires=0)
    return qp.expval(qp.PauliX(0))
\end{lstlisting}

\subsection{Technology Stack and Training Protocol}

\begin{table}[ht]
\centering\small
\begin{tabularx}{\textwidth}{@{} l l X @{}}
  \toprule
  \textbf{Component} & \textbf{Tool} & \textbf{Role} \\
  \midrule
  Quantum simulation (reference) & PennyLane 0.44+   & Differentiable QNode frontend; plugins available for Qiskit/Cirq/others \\
  Analytic baselines (comparison) & PennyLane \texttt{poly\_to\_angles}, Chao/\texttt{pyqsp} & Mapped flat-circuit evaluation; native solver + audit metrics \\
  Automatic diff.     & JAX 0.4.25+ (x64) & Gradient computation and JIT compilation \\
  Optimization        & Optax (Adam)       & Parameter updates, lr~$=0.05$, 500 steps \\
  Vectorization       & \texttt{jax.vmap} & Parallel evaluation over the signal grid \\
  Precision           & float64            & Numerical stability of $\arccos$ near $\pm 1$ \\
  \bottomrule
\end{tabularx}
\caption{Technology stack for the reference implementation. PennyLane is not mandatory for the method; see repository documentation for alternative SDKs.}
\label{tab:stack}
\end{table}

Phase angles are initialized uniformly in $[-0.5,0.5]$. A uniform grid of 64 signal points on $[-0.95,0.95]$ is used; the loss and gradient step are JIT-compiled, and circuit evaluations are batched via \texttt{jax.vmap}. We additionally ablate learning rate, grid density, and initialization range (18 configs, seed~0; \texttt{results/ablation/})---reported briefly in Section~\ref{sec:ablation}.

%% ============================================================
\section{Results}
%% ============================================================

All numerical values in this section are hardcoded in \texttt{manuscript\_numbers.tex} (included at compile time). Provenance: \texttt{results/paper/}, \texttt{results/t1\_degree5/}, \texttt{results/t1\_degree7/}, \texttt{results/t1\_degree15/}, \texttt{results/scaling/}, and \texttt{results/ablation/} JSON/CSV logs. Regenerate figure coordinates with \texttt{py -3.13 -m experiments.generate\_manuscript\_numbers} after targeted sweeps (e.g.\ \texttt{sweep multi-seed --degrees 7,15}).

\subsection{Convergence (Reference Seed)}

Starting from random initialization (loss $\approx1.36$), Adam reduces train MSE by more than four orders of magnitude within 500 steps. The learned phase angles for seed~0 are
\begin{equation}
  \boldsymbol{\phi}^* \approx (-0.0815,\ 1.1463,\ -0.1278,\ 0.1438,\ 0.1317,\ 0.0279),
\end{equation}
with train MSE $9.6\times10^{-5}$, hold-out MSE $1.66\times10^{-3}$, and maximum pointwise error $3.42\times10^{-2}$ against $P_{\mathrm{target}}$ on the training grid.

\begin{figure}[ht]
\centering
\begin{tikzpicture}
\begin{semilogyaxis}[
  width=0.72\textwidth, height=5.5cm,
  xlabel={Training Step},
  ylabel={MSE Loss},
  xmin=0, xmax=520,
  ymin=5e-5, ymax=3,
  xtick={0,100,200,300,400,500},
  grid=both,
  grid style={line width=0.3pt, draw=lightrule},
  major grid style={line width=0.4pt, draw=lightrule},
  axis line style={accent},
  tick style={accent},
  xlabel style={font=\small},
  ylabel style={font=\small},
  tick label style={font=\small, color=graytext},
]
\addplot[accent, thick] coordinates {\LossCurveCoords};
\addplot[only marks, mark=*, mark size=2.5pt, accent] coordinates {(0,1.361)};
\addplot[only marks, mark=o, mark size=2.5pt, accent!70] coordinates {(100,5.75e-4)};
\addplot[only marks, mark=square*, mark size=2.5pt, black!70] coordinates {(500,9.64e-5)};
\node[font=\scriptsize, anchor=west] at (axis cs:4,1.36) {Init: $1.36$};
\node[font=\scriptsize, anchor=south west] at (axis cs:102,6.5e-4) {Step 100: $5.7\times10^{-4}$};
\node[font=\scriptsize, anchor=south west, text=black!70] at (axis cs:355,1.3e-4) {Final: $9.6\times10^{-5}$};
\end{semilogyaxis}
\end{tikzpicture}
\caption{Training loss (log scale) over 500 Adam steps (seed~0, degree~5). Coordinates from \texttt{results/paper/loss\_curve\_d5\_seed0.json}.}
\label{fig:loss}
\end{figure}

\subsection{Multi-Seed Reliability}

We repeated training for 30 independent seeds at degree~5. Train MSE exhibits a long tail: median $6.3\times10^{-5}$, mean $1.6\times10^{-4}$, standard deviation $1.8\times10^{-4}$ (hold-out median $1.5\times10^{-3}$). All seeds converged below the protocol success threshold ($10^{-3}$).

\begin{figure}[ht]
\centering
\begin{tikzpicture}
\begin{semilogyaxis}[
  width=0.72\textwidth, height=4.8cm,
  ylabel={Train MSE},
  xlabel={Seed index},
  xmin=-1, xmax=30,
  xtick={0,5,10,15,20,25,29},
  ymin=3e-5, ymax=1.2e-3,
  grid=both,
  grid style={line width=0.3pt, draw=lightrule},
  axis line style={accent},
  tick label style={font=\small, color=graytext},
  ylabel style={font=\small},
  xlabel style={font=\small},
]
\addplot[accent, only marks, mark=*, mark size=1.8pt, opacity=0.85]
  coordinates {\MultiSeedCoords};
\addplot[accent, dashed, thick] coordinates {(-1,6.3e-5) (30,6.3e-5)};
\node[font=\scriptsize, anchor=north west, accent] at (axis cs:22,9e-5) {median $6.3\times10^{-5}$};
\end{semilogyaxis}
\end{tikzpicture}
\caption{Per-seed train MSE at degree~5 ($n=30$). Points from \texttt{results/t1\_degree5/train\_seed*.json}; dashed line: median.}
\label{fig:multiseed}
\end{figure}

\subsection{Multi-Seed at Higher Degree}
\label{sec:multiseed_higher}

To assess reliability beyond the degree-5 headline benchmark, we repeated the default protocol ($500$ Adam steps, lr~$0.05$, uniform init in $[-0.5,0.5]$) for $n=\MultiSeedD7NSeeds$ independent seeds at $d=7$ and $d=15$. Table~\ref{tab:multiseed_higher} summarizes train and hold-out MSE; Figures~\ref{fig:multiseed_d7} and~\ref{fig:multiseed_d15} mirror the scatter format of Figure~\ref{fig:multiseed}. All seeds at both degrees converged below the protocol success threshold ($10^{-3}$ train MSE).

\begin{table}[ht]
\centering\small
\begin{tabular}{@{} r r r r r r @{}}
  \toprule
  \textbf{$d$} & \textbf{Train median} & \textbf{Train mean} & \textbf{Train stdev} & \textbf{Hold-out median} & \textbf{Success ($<10^{-3}$)} \\
  \midrule
  7  & $\MultiSeedD7TrainMedian$  & $\MultiSeedD7TrainMean$  & $\MultiSeedD7TrainStdev$  & $\MultiSeedD7HoldoutMedian$  & \MultiSeedD7NSuccess/\MultiSeedD7NSeeds \\
  15 & $\MultiSeedD15TrainMedian$ & $\MultiSeedD15TrainMean$ & $\MultiSeedD15TrainStdev$ & $\MultiSeedD15HoldoutMedian$ & \MultiSeedD15NSuccess/\MultiSeedD15NSeeds \\
  \bottomrule
\end{tabular}
\caption{Multi-seed statistics at $d=7$ and $d=15$ (T1, default protocol). Source: \texttt{results/t1\_degree7/summary.json}, \texttt{results/t1\_degree15/summary.json}.}
\label{tab:multiseed_higher}
\end{table}

\begin{figure}[ht]
\centering
\begin{tikzpicture}
\begin{semilogyaxis}[
  width=0.72\textwidth, height=4.2cm,
  ylabel={Train MSE},
  xlabel={Seed index},
  xmin=-1, xmax=30,
  xtick={0,5,10,15,20,25,29},
  ymin=\MultiSeedD7YMin, ymax=\MultiSeedD7YMax,
  grid=both,
  grid style={line width=0.3pt, draw=lightrule},
  axis line style={accent},
  tick label style={font=\small, color=graytext},
  ylabel style={font=\small},
  xlabel style={font=\small},
]
\addplot[accent, only marks, mark=*, mark size=1.8pt, opacity=0.85]
  coordinates {\MultiSeedD7Coords};
\addplot[accent, dashed, thick] coordinates {(-1,\MultiSeedD7TrainMedianNum) (30,\MultiSeedD7TrainMedianNum)};
\end{semilogyaxis}
\end{tikzpicture}
\caption{Per-seed train MSE at degree~7 ($n=\MultiSeedD7NSeeds$). Dashed line: median $\MultiSeedD7TrainMedian$.}
\label{fig:multiseed_d7}
\end{figure}

\begin{figure}[ht]
\centering
\begin{tikzpicture}
\begin{semilogyaxis}[
  width=0.72\textwidth, height=4.2cm,
  ylabel={Train MSE},
  xlabel={Seed index},
  xmin=-1, xmax=30,
  xtick={0,5,10,15,20,25,29},
  ymin=\MultiSeedD15YMin, ymax=\MultiSeedD15YMax,
  grid=both,
  grid style={line width=0.3pt, draw=lightrule},
  axis line style={accent},
  tick label style={font=\small, color=graytext},
  ylabel style={font=\small},
  xlabel style={font=\small},
]
\addplot[accent, only marks, mark=*, mark size=1.8pt, opacity=0.85]
  coordinates {\MultiSeedD15Coords};
\addplot[accent, dashed, thick] coordinates {(-1,\MultiSeedD15TrainMedianNum) (30,\MultiSeedD15TrainMedianNum)};
\end{semilogyaxis}
\end{tikzpicture}
\caption{Per-seed train MSE at degree~15 ($n=\MultiSeedD15NSeeds$). Dashed line: median $\MultiSeedD15TrainMedian$.}
\label{fig:multiseed_d15}
\end{figure}

\subsection{Hyperparameter Ablation}
\label{sec:ablation}

We swept learning rate $\in\{0.01,0.05,0.1\}$, training grid size $\in\{32,64,128\}$, and uniform initialization in $[-0.5,0.5]$ or $[-1,1]$ (18 configs, seed~0). All runs converged below the protocol threshold ($10^{-3}$ train MSE); train MSE ranged from $3.7\times10^{-5}$ to $1.8\times10^{-4}$ (Table~\ref{tab:ablation}). The default protocol ($\mathrm{lr}=0.05$, 64 points, init $\pm0.5$) is mid-pack rather than tuned; headline results are not sensitive to these hyperparameters on T1 at $d=5$.

\begin{table}[ht]
\centering\small
\begin{tabular}{@{} r r l r @{}}
  \toprule
  \textbf{lr} & \textbf{Grid} & \textbf{Init range} & \textbf{Train MSE} \\
  \midrule
  0.05 & 64 & $[-0.5,0.5]$ & $9.6\times10^{-5}$ (default) \\
  0.10 & 128 & $[-1,1]$ & $3.7\times10^{-5}$ (best) \\
  0.01 & 32 & $[-1,1]$ & $1.8\times10^{-4}$ (worst) \\
  \bottomrule
\end{tabular}
\caption{Representative ablation rows (seed~0, $d=5$). Full grid in \texttt{results/ablation/ablation\_table.csv}.}
\label{tab:ablation}
\end{table}

\subsection{Baseline Comparison}

Table~\ref{tab:baseline} compares gradient Adam to analytic solvers on the \emph{same flat training circuit}. Mapped train MSE applies the convention bridge $\phi_{\mathrm{flat}}=\pi/2-\phi_{\mathrm{chao}}$ (Section~\ref{sec:limitations}); raw solver angles without mapping yield much higher MSE (Table caption).

We audited the Chao row against pyqsp's \texttt{sym\_qsp} solver (machine-precision native reconstruction, $\lesssim10^{-15}$ max error at $d=5$). After the same flat-circuit mapping, \texttt{sym\_qsp} mapped train MSE remains $\approx4.7\times10^{-3}$---indistinguishable from the default Laurent completion row. Headline baselines therefore report Laurent (faster); the mapped MSE gap vs.\ gradient is a \emph{convention/capitalization residual}, not solver inaccuracy (\texttt{results/paper/chao\_method\_comparison\_d5.json}).

\begin{table}[ht]
\centering\small
\begin{tabular}{@{} l r r r @{}}
  \toprule
  \textbf{Method} & \textbf{Train MSE (mapped)} & \textbf{Hold-out MSE} & \textbf{Wall-clock (s)} \\
  \midrule
  Gradient Adam (seed 0) & $9.6\times10^{-5}$ & $1.66\times10^{-3}$ & 1.45 \\
  PennyLane \texttt{poly\_to\_angles} & $4.7\times10^{-3}$ & $9.0\times10^{-3}$ & 1.94 \\
  Chao / \texttt{pyqsp} (Laurent) & $4.7\times10^{-3}$ & $9.0\times10^{-3}$ & 0.007 \\
  \bottomrule
\end{tabular}
\caption{Baseline comparison at degree~5, seed~0. Mapped analytic rows use the shared Chebyshev$\to$pyqsp$\to$flat convention. Without mapping, raw PennyLane and Chao (Laurent) train MSE are $0.59$ and $0.95$ respectively. Chao Laurent native reconstruction error is $1.3\times10^{-4}$; \texttt{sym\_qsp} native error is $\lesssim10^{-15}$ with the same mapped train MSE ($\approx4.7\times10^{-3}$).}
\label{tab:baseline}
\end{table}

\begin{figure}[ht]
\centering
\begin{tikzpicture}
\begin{semilogyaxis}[
  width=0.62\textwidth, height=5cm,
  ylabel={Train MSE (mapped)},
  symbolic x coords={Gradient,PennyLane,Chao},
  xtick=data,
  x tick label style={font=\small, rotate=0, anchor=north},
  ymin=5e-5, ymax=2e-2,
  grid=both,
  grid style={line width=0.3pt, draw=lightrule},
  axis line style={accent},
  tick label style={font=\small, color=graytext},
  ylabel style={font=\small},
  enlarge x limits=0.25,
]
\addplot[accent, only marks, mark=*, mark size=5pt] coordinates {
  (Gradient,9.6e-5) (PennyLane,4.7e-3) (Chao,4.7e-3)
};
\end{semilogyaxis}
\end{tikzpicture}
\caption{Mapped train MSE for gradient vs.\ analytic baselines (degree~5, seed~0). Values from \texttt{results/paper/baseline\_comparison\_d5.json}.}
\label{fig:baseline}
\end{figure}

\subsection{Off-Grid Generalization}
\label{sec:offgrid}

Train and hold-out MSE in Table~\ref{tab:baseline} use fixed uniform grids. To probe generalization between grid points, we draw $n=\OffgridNPoints$ uniform random samples in $[-0.95,0.95]$ that do not coincide with the $64$-point training grid (off-grid seed~42) and report maximum pointwise error $| \langle X\rangle_{\boldsymbol{\phi}}(x) - P_{\mathrm{target}}(x) |$ (Table~\ref{tab:offgrid}). Learned phases achieve off-grid max error $\OffgridLearnedMax$, below mapped analytic baselines ($\OffgridPennyLaneMax$ / $\OffgridChaoMax$) and consistent with the training-grid max error ($3.4\times10^{-2}$ in Table~\ref{tab:baseline} row~1). Source: \texttt{results/paper/offgrid\_random\_d5\_seed0.json} via \texttt{experiments.offgrid\_eval}.

\begin{table}[ht]
\centering\small
\begin{tabular}{@{} l r @{}}
  \toprule
  \textbf{Method} & \textbf{Off-grid max error} \\
  \midrule
  Gradient Adam (seed 0) & $\OffgridLearnedMax$ \\
  PennyLane \texttt{poly\_to\_angles} (mapped) & $\OffgridPennyLaneMax$ \\
  Chao / \texttt{pyqsp} Laurent (mapped) & $\OffgridChaoMax$ \\
  \bottomrule
\end{tabular}
\caption{Random off-grid max pointwise error at $d=5$ ($n=\OffgridNPoints$ samples, domain $[-0.95,0.95]$, excluding train-grid hits).}
\label{tab:offgrid}
\end{table}

\subsection{Degree Scaling}

Figure~\ref{fig:scaling} summarizes seed~0 training at odd degrees $\{5,7,9,15,21\}$. Gradient training remains below $10^{-3}$ train MSE through $d=21$ in this protocol; mapped analytic MSE is higher ($\sim10^{-2}$--$10^{-1}$ at moderate $d$), consistent with capitalization and convention residuals (Section~\ref{sec:limitations}).

\begin{figure}[ht]
\centering
\begin{tikzpicture}
\begin{semilogyaxis}[
  width=0.72\textwidth, height=5.5cm,
  xlabel={Polynomial degree $d$},
  ylabel={Train MSE (mapped)},
  xmode=normal,
  xmin=4, xmax=22,
  xtick={5,7,9,15,21},
  ymin=1e-6, ymax=1,
  grid=both,
  grid style={line width=0.3pt, draw=lightrule},
  axis line style={accent},
  tick label style={font=\small, color=graytext},
  xlabel style={font=\small},
  ylabel style={font=\small},
  legend style={font=\small, at={(0.02,0.98)}, anchor=north west,
                draw=lightrule, fill=white},
]
\addplot[accent, thick, mark=*, mark size=2.5pt]
  coordinates {\ScalingLearnedCoords};
\addlegendentry{Gradient Adam}
\addplot[black!70, thick, dashed, mark=square*, mark size=2.5pt]
  coordinates {\ScalingAnalyticCoords};
\addlegendentry{Analytic (mapped)}
\end{semilogyaxis}
\end{tikzpicture}
\caption{Degree scaling (seed~0): learned vs.\ mapped analytic train MSE. From \texttt{results/scaling/scaling\_table.csv}.}
\label{fig:scaling}
\end{figure}

\subsection{Polynomial Fidelity}

Figure~\ref{fig:poly} shows the Chebyshev target, the trained circuit output $\langle X\rangle_{\boldsymbol{\phi}^*}(x)$ evaluated on a dense grid (not a synthetic perturbation), and the pointwise residual. The learned curve tracks $P_{\mathrm{target}}$ closely; visible structure in the residual panel reflects optimization on a discrete training grid and hold-out effects, not degree-5 truncation of $\sin(x)$.

\begin{figure}[ht]
\centering
\begin{tikzpicture}
\begin{groupplot}[
  group style={
    group size=1 by 2,
    vertical sep=0.38cm,
    xticklabels at=edge bottom,
  },
  width=0.72\textwidth,
  height=5.2cm,
  xmin=-1.02, xmax=1.02,
  xtick={-1,-0.5,0,0.5,1},
  grid=both,
  grid style={line width=0.3pt, draw=lightrule},
  axis line style={-Stealth, thin, graytext},
  tick label style={font=\small, color=graytext},
  xlabel style={font=\small},
  ylabel style={font=\small},
]
\nextgroupplot[
  ylabel={$P(x)$},
  ymin=-1.05, ymax=1.05,
  legend style={at={(0.97,0.95)}, anchor=north east, font=\scriptsize,
                draw=lightrule, fill=white},
]
\addplot[gray, dashed, thick, domain=-0.95:0.95, samples=100]
  {1.5257*x - 0.9416*x^3 + 0.2528*x^5};
\addlegendentry{$P_{\mathrm{target}}$ (Chebyshev)}
\addplot[accent, thick, densely dashed]
  coordinates {\PolyLearnedCoords};
\addlegendentry{Trained $\langle X\rangle$}
\nextgroupplot[
  xlabel={Signal $x$},
  ylabel={Residual},
  ymin=-0.06, ymax=0.06,
]
\addplot[accent!70, thick]
  coordinates {\PolyResidualCoords};
\addplot[gray!50, thin] coordinates {(-1,0) (1,0)};
\end{groupplot}
\end{tikzpicture}
\caption{Polynomial fidelity (seed~0). \textbf{Top:} Chebyshev target vs.\ circuit output from learned phases in \texttt{results/paper/baseline\_comparison\_d5.json}. \textbf{Bottom:} pointwise residual on a dense grid.}
\label{fig:poly}
\end{figure}

%% ============================================================
\section{Discussion}
%% ============================================================

This section interprets the benchmark results above. We do not claim a universally superior QSP solver; we compare approaches on the \emph{flat-circuit MSE} used for training and on reproducibility criteria (convention mapping, multi-seed reliability, scaling tables).

\subsection{Analytic Solvers vs.\ Gradient Training}

\begin{table}[ht]
\centering\small
\begin{tabularx}{\textwidth}{@{} X l @{}}
  \toprule
  \textbf{Scenario} & \textbf{Recommended approach} \\
  \midrule
  Known target, flat-circuit training metric (this work) & Gradient training \\
  Fast classical angles; native solver convention & Chao / \texttt{pyqsp} or PennyLane \\
  Implicit loss (e.g.\ energy in a VQA) & End-to-end gradient training \\
  Very high degree, closed-form target & Iterative analytic solver~\cite{chao2020finding} \\
  Novel polynomial, no closed form & Gradient training \\
  \bottomrule
\end{tabularx}
\caption{Decision guide updated from measured baselines (Section~4). Analytic solvers excel in speed and native accuracy; gradient training wins on the flat-circuit MSE used for learning.}
\label{tab:decision}
\end{table}

\subsection{Loss Landscape and Scaling}

The QSP loss landscape is non-convex. At degree~5, 30/30 seeds converged below $10^{-3}$ train MSE with uniform initialization in $[-0.5,0.5]$; the same holds at $d=7$ and $d=15$ under the default protocol (Section~\ref{sec:multiseed_higher}). At $d=15$, seed~0 hold-out error in the scaling snapshot ($\sim2.3\times10^{-2}$) exceeds the multi-seed hold-out median ($\MultiSeedD15HoldoutMedian$), indicating seed-dependent generalization on the dense hold-out grid rather than systematic optimization failure.

\subsection{The JAX Traceability Constraint}

The flat-circuit pattern described in Section~3.3 is the correct workaround for JAX-based variational optimization of QSP circuits, and is expected to generalize to multi-qubit block-encoding extensions and to other quantum SDKs when the same traceability rules are respected. JAX transforms functions by tracing them with abstract values; any component that captures concrete arrays at construction time exits the trace. This is not unique to PennyLane---it is a general pitfall for template-heavy circuit APIs combined with JAX-style AD.

\subsection{Reproducibility}

The implementation, experiment scripts, audit trail (\texttt{docs/AUDIT\_TRAIL.md}), and hardcoded figure values (\texttt{manuscript\_numbers.tex}) are available at \url{https://github.com/rosspeili/trainable-qsp-angles}~\cite{peili_trainable_qsp_2026} (DOI: \href{https://doi.org/10.5281/zenodo.20645403}{10.5281/zenodo.20645403}). Regenerate \texttt{manuscript\_numbers.tex} with \texttt{py -3.13 -m experiments.sweep all}, \texttt{py -3.13 -m experiments.summarize baseline}, and \texttt{py -3.13 -m experiments.generate\_manuscript\_numbers}.

\subsection{Limitations}
\label{sec:limitations}

\begin{itemize}\setlength\itemsep{2pt}
  \item \textbf{Convention mapping.} Analytic baselines require $\phi_{\mathrm{flat}}=\pi/2-\phi_{\mathrm{chao}}$ for fair comparison on our flat circuit. Mapped analytic MSE remains above gradient MSE ($\sim10^{-3}$ at $d=5$, higher at some degrees) even when pyqsp \texttt{sym\_qsp} achieves machine-precision native reconstruction; the residual reflects capitalization and the incomplete bridge to $\mathrm{RZ}(-2\phi)$/$\langle X\rangle$, not failure of the Chao solver in its native convention.
  \item \textbf{Single target family.} Results apply to one Chebyshev $\sin$ benchmark (T1); other polynomials may behave differently.
  \item \textbf{Simulation only.} No shot noise, hardware, or transpilation effects.
  \item \textbf{Scaling statistics.} Degree scaling curve (Figure~\ref{fig:scaling}) uses one seed per degree; multi-seed statistics are reported at $d=5$ (Figure~\ref{fig:multiseed}) and $d\in\{7,15\}$ (Section~\ref{sec:multiseed_higher}).
  \item \textbf{Ablation scope.} Hyperparameter ablation (Section~\ref{sec:ablation}) uses seed~0 only; multi-seed stats cover the default protocol at $d=5$.
  \item \textbf{Off-grid probe.} Section~\ref{sec:offgrid} uses one random off-grid draw (1024 points, seed~42) at $d=5$ only; it is not a hold-out set for hyperparameter tuning.
  \item \textbf{Reference stack.} One PennyLane+JAX implementation; the flat-circuit recipe is portable but not cross-validated on hardware here.
\end{itemize}

\section{Outlook}

Within the scope of this benchmark, gradient-learned QSP phases are most compelling when the training objective \emph{is} the flat differentiable circuit---for example, QSP blocks inside variational pipelines with implicit targets. Immediate extensions include optional cross-SDK validation and barren-plateau analysis at higher degree. The combination of interpretable polynomial structure and automatic differentiation remains a practical template for quantum subroutine design when the loss is end-to-end differentiable.

%% ============================================================
%% (Former extended speculative section condensed into Outlook above.)
%% ============================================================








\nocite{*}
\printbibliography



\end{document}
